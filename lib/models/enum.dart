// lib/models/enums.dart (或类似文件)
enum ObjectType { plant, pet }

enum Gender { male, female, unknown }
// 可以为常见的 Log EventType 定义枚举，但初期用 String 也可
// enum PlantEventType { watering, fertilizing, repotting, pruning, pestControl, lightChange }
// enum PetEventType { feeding, medication, vaccination, deworming, grooming, weight, vetVisit, behavior }